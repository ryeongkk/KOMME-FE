import { CourseRouteMap } from "@/components/course/course-route-map";

export default function CourseRouteMapPage() {
  return (
    <main className="relative flex flex-1 flex-col bg-white">
      <CourseRouteMap />
    </main>
  );
}
